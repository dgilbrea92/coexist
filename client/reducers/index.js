import { combineReducers } from 'redux';

// import reducer(s) function(s) once written

const reducers = combineReducers({
  // name for reducers : // imported reducers function
});

export default reducers;
